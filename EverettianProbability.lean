import QuantumFoundations.BornRule.Assembly

/-!
**FR.** Point d'entrée du paquet `EverettianProbability`. Squelette minimal
de jalon P1 : un seul import trivial, servant à vérifier que le pin Lake vers
`quantum_foundations` (et transitivement `gleason`, `mathlib`) résout et
compile avant toute autre écriture.

**EN.** Entry point of the `EverettianProbability` package. Minimal P1
skeleton: a single trivial import, used to verify that the Lake pin to
`quantum_foundations` (and transitively `gleason`, `mathlib`) resolves and
builds before any further writing.
-/
