import QuantumFoundations.EverettianAPI

/-!
**FR.** # Fronti?re stable vers `quantum-foundations-lean`

Ce module est la fronti?re d'import recommand?e pour les API amont stables
utilis?es par ce d?p?t. Il r?exporte uniquement l'ensemble amont
`QuantumFoundations.EverettianAPI` : aucune d?finition, aucun alias et aucun
r?sultat scientifique nouveau n'y sont introduits. Les namespaces amont restent
les namespaces de r?f?rence.

**EN.** # Stable boundary to `quantum-foundations-lean`

This module is the recommended import boundary for the stable upstream APIs
used by this repository. It re-exports only the upstream
`QuantumFoundations.EverettianAPI` bundle: it introduces no definition, alias,
or new scientific result. Upstream namespaces remain the reference namespaces.

-/
